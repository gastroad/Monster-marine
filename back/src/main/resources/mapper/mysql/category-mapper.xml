<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.monstermarine.api.category.CategoryMapper">

	<select id="getCategory" resultType="categoryVO" parameterType="categoryVO">
		/*
			SQL ID : com.monstermarine.api.category.getCategory
			설명 : 카테고리 목록 조회
		*/
		SELECT
			CATEGORY_ID,
			GROUP_BY,
			IS_USE,
			NAME,
			DEPTH1,
			DEPTH2,
			ORDER_BY
		FROM
			MONSTER_MARINE.CATEGORY
		WHERE
			CATEGORY_ID = #{categoryId}
	</select>

	<select id="getCategoryList" resultType="java.util.HashMap" parameterType="categoryVO">
		/*
			SQL ID : com.monstermarine.api.category.getCategory
			설명 : 카테고리 목록 조회
		*/v
		SELECT
			CATEGORY_ID,
			GROUP_BY,
			IS_USE,
			NAME,
			DEPTH1,
			DEPTH2,
			ORDER_BY
		FROM
			MONSTER_MARINE.CATEGORY
	</select>

	<select id="getCategoryDepth1" resultType="java.util.HashMap" parameterType="categoryVO">
		/*
			SQL ID : com.monstermarine.api.category.getCategoryDepth1
			설명 : 카테고리 DEPTH1 조회
		*/
		SELECT
			CATEGORY_ID,
			NAME
		FROM
			MONSTER_MARINE.CATEGORY
		WHERE
			DEPTH2 = "00"
	</select>

	<select id="getCategoryDepth2" resultType="java.util.HashMap" parameterType="categoryVO">
		/*
			SQL ID : com.monstermarine.api.category.getCategoryDepth2
			설명 : 카테고리 DEPTH2 조회
		*/
		SELECT
			CATEGORY_ID,
			NAME
		FROM
			MONSTER_MARINE.CATEGORY
		<where>
			<if test="depth1 eq null and depth1 eq ''">AND DEPTH1 = #{depth1}</if>
			<if test="depth2 eq null and depth2 eq ''">AND DEPTH2 = #{depth2}</if>
			<if test="isUse eq null and isUse eq ''">AND IS_USE = TRUE</if>
		</where>
	</select>

</mapper>
